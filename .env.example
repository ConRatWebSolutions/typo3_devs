# Email Service (SMTP)
# Your SMTP server hostname (e.g., smtp.strato.de, smtp.ionos.de, smtp.gmail.com)
SMTP_HOST="smtp.ionos.de"
# SMTP port (587 for TLS, 465 for SSL, 25 for non-secure)
SMTP_PORT="587"
# Use secure connection (true for port 465, false for port 587)
SMTP_SECURE="false"
# Your SMTP username (usually your email address)
SMTP_USER="info@somesolutions.de"
# Your SMTP password or app-specific password
SMTP_PASSWORD="Hgdzd665##Okdjjs23"

# Email addresses
# The email address that will appear as the sender
EMAIL_FROM="noreply@somesolutions.de"
# The email address that will receive contact form submissions
EMAIL_TO="info@somesolutions.de"

# Next.js
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
